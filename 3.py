# Необходимо написать скрипт, который будет брать строку и объединять с другой строкой
# Использовать перменные, которые определят значение каждой строки
# При объединении (конкатенации) строк, необходимо помнить, что у слов есть ПРОБЕЛЫ
# Эти переменные тоже через ввод с клавиатуры после запуска

firstString = str(input('enter the word(s): '))
secondString = str(input('enter the word(s): '))

stringResult = firstString + ' ' + secondString
print(stringResult)